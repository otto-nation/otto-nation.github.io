# Regenerated by both `next dev` and `next build`, and they disagree: dev writes
# `./.next/dev/types/...`, build writes `./.next/types/...`. Tracking it means
# whichever command ran last leaves a dirty tree. tsconfig.json includes the
# build form directly, so nothing depends on which form is on disk.
next-env.d.ts
